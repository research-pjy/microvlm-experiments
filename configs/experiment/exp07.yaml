# @package _global_
# Experiment 07 — visual encoder ambiguity (fully runnable on the fixture).
defaults:
  - /model: mini
  - /model/encoder: arch_a_patch_then_conv
  - /data: shortdesc
  - /teacher: local_ollama
  - /training: local_smoke
  - /evaluation: metrics
  - _self_

experiment_id: exp07
encoder_arches:
  - arch_a
  - arch_b
